name: Deploy

on:
  workflow_dispatch:

jobs:
  Deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2 appleboy/ssh-action@v1.0.3
      - name: Build & Deploy
        with:
          host: "18.192.213.152"
          username: "ubuntu"
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: "22"
          script: |
            whoami
            ls -al 
        
